[build]
  command = "npm ci && npm run build"
  publish = "dist"

# Vite 8 은 Node 20.19+ / 22.12+ 를 요구한다. 빌드 이미지 기본값에 맡기면
# 이미지가 바뀔 때 조용히 깨지므로 고정한다.
[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
